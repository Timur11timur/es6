array.forEach(function(task) {
     return console.log(task);
});

array.forEach((task) => {
    return console.log(task)
});

array.forEach(task => {
    return console.log(task);
});

array.forEach(task => console.log(task));
